<!DOCTYPE html>
<html lang="{{ i18n.locale }}">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ i18n.t('admin.title') }} — {{ i18n.t('site.title') }}</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>
  <body class="min-h-screen bg-stone-50 font-serif text-stone-800 antialiased">
    <header class="border-b border-stone-200 bg-white">
      <div class="mx-auto flex h-14 max-w-5xl items-center justify-between px-6">
        <a href="/" class="font-display text-lg tracking-tight text-stone-900">poesia.pics</a>
        <nav class="flex items-center gap-6 text-sm">
          <a href="/admin/moods" class="text-stone-600 hover:text-stone-900">{{ i18n.t('admin.moods') }}</a>
          <a href="/admin/voices" class="text-stone-600 hover:text-stone-900">{{ i18n.t('admin.voices') }}</a>
          <a href="/admin/poems" class="text-stone-600 hover:text-stone-900">{{ i18n.t('admin.poems') }}</a>
        </nav>
      </div>
    </header>
    <main class="mx-auto max-w-5xl px-6 py-8">
      @include('partials/flash_alerts')
      {{{ await $slots.main() }}}
    </main>
  </body>
</html>
